# Verify token — must match Meta → Webhooks → Verify Token
WEBHOOK_VERIFY_TOKEN=meta_poc_verify_token

# App secret (App settings → Basic) — validates webhook POST signatures
FB_APP_SECRET=

# Optional: your public ngrok URL (no trailing slash), e.g. https://xxxx.ngrok-free.app
WEBHOOK_PUBLIC_URL=
